<% include partials/header %>
                    <div class="profile-container">
                        <p class="label">Edit Profile</p>
                        <div class="profile-container-content">
                            <div class="profile-container-content-heading">
                                <p>Full Name</p>
                                <p>Profile Pic</p>
                                <p>Phone</p>
                                <p>Address</p>
                                <!-- <p>Email</p> -->
                                <p>Gender</p>
                                <p>Place Born</p>
                                <p>Date Born</p>
                                <p>Job</p>
                                <p>Institution</p>
                            </div>
                            <div class="profile-container-content-details">
                                <form action="/profile/update" method="POST">
                                    <input class="profile-input" type="text" name="fullname" placeholder="Full Name" required  value="<%= results[results.length-1].fullname %>">
                                    <input class="profile-input" type="text" name="profilepic" placeholder="Url" value="<%= results[results.length-1].profilepic %>">
                                    <input class="profile-input" type="text" name="phone" placeholder="Phone" required value="<%= results[results.length-1].phone %>">
                                    <input class="profile-input" type="text" name="address" placeholder="Address" required value="<%= results[results.length-1].address %>">
                                    <!-- <input class="profile-input" type="text" name="email" placeholder="Email" required> -->
                                    <input class="profile-input" type="text" name="gender" placeholder="Gender" required value="<%= results[results.length-1].gender %>">
                                    <input class="profile-input" type="text" name="placeborn" placeholder="Place Born" required value="<%= results[results.length-1].placeborn %>">
                                    <input class="profile-input" type="date" name="dateborn" placeholder="Date Born" required value="<%= results[results.length-1].d %>">
                                    <input class="profile-input" type="text" name="job" placeholder="Job" required value="<%= results[results.length-1].job %>">
                                    <input class="profile-input" type="text" name="institution" placeholder="Institution" required value="<%= results[results.length-1].institution %>">
                                    <br>
                                    <button class="button"><span>Save </span></button>
                                </form>
                            </div>
                        </div>
                    </div>
                    <script>
                        var select = document.getElementById('3')
                        select.onload = load();
                        function load() {
                            select.classList.add('side-nav-item-active');
                        }
                    </script>  
<% include partials/footer %> 